name: listcy
services:
  app:
    container_name: listcy-main
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.production
    ports:
      - '3000:3000'
    env_file:
      - ../env/.env.production
    restart: unless-stopped
